// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Don't specify a version to avoid conflict with buildSrc
    id 'com.android.application' apply false
}